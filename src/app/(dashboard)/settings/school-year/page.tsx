import { SchoolYearSettingsSection } from "@/components/settings/SchoolYearSettingsSection";

export const metadata = {
  title: "School year",
};

/**
 * Settings Ease Phase 3 — School year (exact cream/Fraunces panels).
 */
export default function SchoolYearSettingsPage() {
  return <SchoolYearSettingsSection />;
}
