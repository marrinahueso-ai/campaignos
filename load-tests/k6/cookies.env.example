# Deprecated alias — prefer env.example for the full suite.
# Copy to cookies.env (gitignored) for legacy single-cookie smoke only.

BASE_URL=https://your-staging-or-preview.example
TEST_RUN_ID=k6-adhoc

# Full Cookie header from a logged-in browser session on that same host.
COOKIE=
